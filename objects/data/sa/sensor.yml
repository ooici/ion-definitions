# UNUSED - Describes a physical device that can be deployed as part of an instrument, and reports its
# data through that instrument.
SensorDevice: !Extends_Device
  # Identifier of hard-coded operating system in unit
  firmware_version: ""


# UNUSED -Describes a distinguished type of sensor, derived from the manufacturer's model number and common attributes.
SensorModel: !Extends_DeviceModel
