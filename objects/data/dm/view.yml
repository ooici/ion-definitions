# Specifies a predefined, parametrized search for resources.
# Describes order and additional filters to be applied when view is evaluated.
View: !Extends_InformationResource
  order:   []
  filters: ""

