# Bootstrap process interface definition

name: bootstrap
docstring: Process that bootstraps the ION system
dependencies: [datastore, directory,
  identity_management,
  resource_registry,
  policy_management,
  org_management,
  exchange_management,
  ingestion_management,
  data_retriever,
  transform_management
]
methods:
